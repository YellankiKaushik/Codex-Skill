# Fixtures

Integration tests create their own temporary repositories and local bare remotes at runtime. This directory is intentionally lightweight so the plugin repository has a stable place for any future static fixtures.
